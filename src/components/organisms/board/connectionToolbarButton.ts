/*
 * Shared chrome for the connection toolbar's popover triggers: a frosted button showing a compact
 * summary of its group's current state. Theme tokens only, matching the group toolbar's look.
 */
export const CONNECTION_TRIGGER_CLASS =
   'flex h-7 min-w-7 cursor-pointer items-center justify-center gap-1 rounded px-1.5 text-popover-foreground hover:bg-muted';
